// @ts-nocheck
/** Autofix obscure plant evalmsf9kel5 */
export function evalO15_evalmsf9kel5(obj: Record<string, number>) {
  const keys: string[] = [];
  for (const k in obj) keys.push(k);
  return keys[0]!.toUpperCase();
}

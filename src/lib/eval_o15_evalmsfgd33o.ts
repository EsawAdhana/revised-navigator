// @ts-nocheck
/** Autofix obscure plant evalmsfgd33o */
export function evalO15_evalmsfgd33o(obj: Record<string, number>) {
  const keys: string[] = [];
  for (const k in obj) keys.push(k);
  return keys[0]!.toUpperCase();
}
